import * as assert from "assert";

import Inferno from "inferno";
import InfernoTestUtils from "inferno-test-utils";

//import DungeonClient from "../Dungeon/Components/DungeonClient";
//import DebugTools from "../Dungeon/Components/DebugTools";


describe('Dungeons -- High Level', function() {
  describe.skip('Client', function() {
    it("should render the dungeon client without error", (done) => {
      /*let client = Inferno.createElement(DungeonClient, { "store": {} });

      assert.ok(InfernoTestUtils.isVNode(client));*/
      assert.ok(true);
      done();
    });
  });


  describe.skip('Debug Tools', function() {
    it("should render the dungeon debug tools without error", (done) => {
      /*let client = Inferno.createElement(DebugTools, { "store": {} });

      assert.ok(InfernoTestUtils.isVNode(client));*/
      assert.ok(true);
      done();
    });
  });
});
