# This requirements file lists all third-party dependencies for this project.
#
# Run 'pip install -r requirements.txt -t lib/' to install these dependencies
# in `lib/` subdirectory.
#
# Note: The `lib` directory is added to `sys.path` by `appengine_config.py`.
#
# bcrypt dependency deferred in favor of manually adding a version that works.
Flask==0.10
python-dateutil
Flask-WTF
git+https://github.com/brmullikin/flask-markdown
mdx_linkify
flask_sqlalchemy

# Local dependencies (Stuff we don't need in prod).
pillow
flake8
